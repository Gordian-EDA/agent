[package]
name = "greedy-place"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Free-tier greedy hill-climb schematic placement engine: implements sch-place's PlacementEngine and OWNS its objective (the free-tier weighted combo) and its search (refine/polish), calling sch-floorplan's measurement library because it CHOSE a measurement-based method."

[dependencies]
sch-place = { path = "../sch-place" }
sch-floorplan = { path = "../sch-floorplan" }
