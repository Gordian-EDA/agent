Logic TX/RX and bus CANH/CANL must connect to the correct transceiver pins. Termination must bridge CANH to CANL and be genuinely switchable, TVS protection must be at the external bus side, and power/decoupling must match the chosen part.
expect: agent_exit == 0
expect: schematic_created == true
expect: part_count >= 7
expect: erc_errors == 0
expect: schematic_critic_score >= 8
expect: human_look_schematic_score >= 8
