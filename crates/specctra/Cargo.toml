[package]
name = "specctra"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Specctra .ses codec for Freerouting routed geometry."

[dependencies]
pcb-model = { path = "../pcb-model" }
kiutils_sexpr.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile.workspace = true
