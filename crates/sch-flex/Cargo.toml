[package]
name = "sch-flex"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Typeset a schematic block from the model's row/col layout tree: measure, align, place."

[dependencies]
sch-model = { path = "../sch-model" }
kicad-symbol = { path = "../kicad-symbol" }
geom = { path = "../geom" }
circuit-graph = { path = "../circuit-graph" }
