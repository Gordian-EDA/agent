[package]
name = "greedy-place"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Free-tier greedy hill-climb schematic placement engine (implements sch-model's PlacementEngine; depends on sch-model alone, scoring through the injected PlacementCost)."

[dependencies]
sch-model = { path = "../sch-model" }
