[package]
name = "sch-io"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Read a .kicad_sch back into the kernel Design/YAML view."

[dependencies]
sch-place = { path = "../sch-place" }
circuit-lang = { path = "../circuit-lang" }
sch-check = { path = "../sch-check" }
kicad = { path = "../kicad" }
