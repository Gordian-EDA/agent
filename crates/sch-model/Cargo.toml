[package]
name = "sch-model"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared schematic vocabulary: geometry (Dir/segment math), grid snapping, deterministic ids, and the emit result types."

[dependencies]
serde = { workspace = true, features = ["derive"] }
uuid.workspace = true
