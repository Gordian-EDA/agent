[package]
name = "grid-astar"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Sequential grid-A* PCB router: one shortest-net-first A* per net over a per-layer occupancy grid."

[dependencies]
pcb-model = { path = "../pcb-model" }
drc-lint = { path = "../drc-lint" }
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
