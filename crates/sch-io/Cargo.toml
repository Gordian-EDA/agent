[package]
name = "sch-io"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Schematic I/O: the elbow wire router, label text solver, the SchematicWriter (Design+placement → .kicad_sch), and read (.kicad_sch → Design)."

[dependencies]
geom = { path = "../geom" }
sch-place = { path = "../sch-place" }
circuit-lang = { path = "../circuit-lang" }
kicad = { path = "../kicad" }
kicad-symbol = { path = "../kicad-symbol" }
kiutils_kicad.workspace = true
uuid.workspace = true
tempfile.workspace = true
