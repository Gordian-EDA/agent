[package]
name = "anneal-place"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Premium simulated-annealing schematic placement engine (implements sch-model's PlacementEngine; scores through the injected PlacementCost, drives sch-place-core's env-free SA scaffold)."

[dependencies]
sch-model = { path = "../sch-model" }
sch-place-core = { path = "../sch-place-core" }
rayon.workspace = true
