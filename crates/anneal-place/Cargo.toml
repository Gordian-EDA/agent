[package]
name = "anneal-place"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Premium simulated-annealing schematic placement engine: implements sch-place's PlacementEngine, declares the premium tier, and OWNS its objective (the premium energy + warning gate) and its search (the SA move-set + proxy costs + multi-start), calling sch-floorplan's measurement library because it CHOSE a measurement-based method."

[dependencies]
sch-place = { path = "../sch-place" }
sch-floorplan = { path = "../sch-floorplan" }
rayon.workspace = true
