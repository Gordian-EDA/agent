[package]
name = "drc-lint"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "In-house PCB DRC oracle: clearance/width/via geometry + connectivity checks over a routed solution."

[dependencies]
pcb-model = { path = "../pcb-model" }
drc-core = { path = "../drc-core" }
serde = { workspace = true, features = ["derive"] }
