In this existing ECC83 preamp schematic, add a 100nF DC-blocking capacitor in series between the input connector P1 pin 2 and the grid of the input triode (U1 pin 2), so the input is AC-coupled. R4 must stay on the connector side as the input load; the grid side must reach U1 pin 2 only through the new capacitor. Preserve every other part, value and position, and check the schematic when you are done.
