The sensor pinout, pull-ups, decoupling, and address state must be correct. The header should sit at an edge, supporting passives should cluster around the sensor, and the compact board must be fully routed and DRC-clean.
expect: agent_exit == 0
expect: schematic_created == true
expect: pcb_created == true
expect: part_count >= 6
expect: erc_errors == 0
expect: drc_errors == 0
expect: unconnected_items == 0
expect: unrouted == []
expect: len(fab_files) >= 3
expect: schematic_critic_score >= 8
expect: pcb_critic_score >= 8
expect: human_look_schematic_score >= 8
expect: human_look_pcb_score >= 8
