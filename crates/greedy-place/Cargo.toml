[package]
name = "greedy-place"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Free-tier greedy hill-climb schematic placement engine: implements sch-floorplan's PlacementEngine and OWNS its objective (the free-tier weighted combo) and its search (refine/polish), over sch-floorplan's routed-sheet measurement library."

[dependencies]
geom = { path = "../geom" }
sch-place = { path = "../sch-place" }
sch-floorplan = { path = "../sch-floorplan" }
