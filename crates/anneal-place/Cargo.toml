[package]
name = "anneal-place"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Premium simulated-annealing schematic placement engine: implements sch-model's PlacementEngine, declares the premium tier, and OWNS its objective (the premium energy + warning gate) and its search (the SA move-set + proxy costs + multi-start), calling sch-place-core's measurement library because it CHOSE a measurement-based method."

[dependencies]
sch-model = { path = "../sch-model" }
sch-place-core = { path = "../sch-place-core" }
rayon.workspace = true
