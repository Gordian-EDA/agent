The project already contains a schematic. Add input protection at the IN connector P1: put a small series resistor in the signal path coming from P1 pin 2, and add a pair of clamp diodes from the protected node to GND. The original signal path must still run end to end, now through the new series resistor. Add nothing else, keep every existing part's reference, and leave every existing part exactly where it is. Render the schematic.
