[package]
name = "sch-place"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared schematic vocabulary: the layout IR, placeable items, the placement SDK, netclass, and emit result types."

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
geom = { path = "../geom" }
kicad-symbol = { path = "../kicad-symbol" }
