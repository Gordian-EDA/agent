The project already contains a schematic. Add a power-present indicator: one LED plus its current-limiting resistor, in series from pin 1 of the POWER connector P3 to GND. Assume that rail is 12 V when sizing the resistor. Add nothing else, keep every existing part's reference, and leave every existing part exactly where it is. Render the schematic.
