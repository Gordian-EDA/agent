P1 must become a three-pin part whose first two pins keep their original nets while pin 3 goes to GND, and nothing else in the circuit may change.
expect: agent_exit == 0
expect: sch_errors == []
expect: erc_errors == 0
expect: unchanged_symbols_moved == []
expect: fields_lost == []
expect: symbols_removed == []
expect: lib_ids_changed != []
expect: net_delta_removed == []
expect: net_delta_pins_now_unconnected == []
expect: len(net_delta_pins_now_connected) >= 1
expect: paths_broken == []
expect: text_collisions_added == []
