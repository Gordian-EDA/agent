[package]
name = "greedy-place"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Free-tier greedy hill-climb schematic placement engine (implements sch-model's PlacementEngine over the sch-place-core scaffold)."

[dependencies]
sch-place-core = { path = "../sch-place-core" }
sch-model = { path = "../sch-model" }
