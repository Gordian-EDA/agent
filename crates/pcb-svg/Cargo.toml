[package]
name = "pcb-svg"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Diagnostic SVG renderer for the PCB engine's own view of a board (placement, routes, failures)."

[dependencies]
pcb-model = { path = "../pcb-model" }
grid-astar = { path = "../grid-astar" }
negotiated-mesh = { path = "../negotiated-mesh" }
pcb-place = { path = "../pcb-place" }
serde_json.workspace = true
