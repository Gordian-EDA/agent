The transceiver's real pin functions must be respected, termination must be switchable across CANH/CANL, and protection must sit at the bus connector.
expect: agent_exit == 0
expect: schematic_created == true
expect: sch_errors == []
expect: erc_errors == 0
expect: partition_matches_kicad == true
expect: part_count >= 18
expect: extractor_warnings == []
