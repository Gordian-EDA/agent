The series element must sit in the signal path rather than beside it, the clamp diodes must be oriented to conduct only on an overvoltage, and no existing part may move.
expect: unchanged_symbols_moved == []
expect: fields_lost == []
expect: erc_errors == 0
expect: symbols_removed == []
expect: len(symbols_added) >= 3
