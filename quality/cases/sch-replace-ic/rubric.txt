U1 must read as an ECC82/12AU7 while keeping its reference, footprint and every pin connection; the rest of the amplifier must be untouched.
expect: unchanged_symbols_moved == []
expect: fields_lost == []
expect: erc_errors == 0
expect: symbols_removed == []
expect: net_delta_unchanged == true
