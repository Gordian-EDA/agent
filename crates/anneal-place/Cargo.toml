[package]
name = "anneal-place"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Premium simulated-annealing schematic placement engine (implements sch-model's PlacementEngine over the sch-place-core scaffold)."

[dependencies]
sch-place-core = { path = "../sch-place-core" }
sch-model = { path = "../sch-model" }
sch-io = { path = "../sch-io" }
kicad-cli-rs = { path = "../kicad-cli-rs" }
rayon.workspace = true
