[package]
name = "pcb-drc-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Pluggable-rule DRC engine SDK for PCB routing: a Rule trait, a composable DrcSuite, and the shared DrcCtx + copper collection rules read."

[dependencies]
pcb-model = { path = "../pcb-model" }
serde = { workspace = true, features = ["derive"] }

[dev-dependencies]
serde_json = { workspace = true }
