[package]
name = "kicad-sexpr"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "KiCAD s-expr file & library access: .kicad_pcb + footprint/symbol libraries, geometry, fuzzy index."

[dependencies]
kicad-symbol = { path = "../kicad-symbol" }
geom = { path = "../geom" }
pcb-model = { path = "../pcb-model" }
kicad-cli = { path = "../kicad-cli" }
strsim.workspace = true
fuzzy-matcher.workspace = true
elsa.workspace = true
serde.workspace = true
serde_json.workspace = true
kiutils_kicad.workspace = true
kiutils_sexpr.workspace = true
tempfile.workspace = true
uuid.workspace = true
quick-xml.workspace = true

[dev-dependencies]
drc-lint = { path = "../drc-lint" }
# DSL-integration tests only: drive `circuit_lang::compile` against the real
# symbol provider. The library no longer depends on circuit-lang (see CB-3).
circuit-lang = { path = "../circuit-lang" }
