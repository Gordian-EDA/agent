The project already contains a schematic. Replace the two-pin input connector P1 (value IN) with a three-pin connector — a stock Connector_Generic:Conn_01x03 is fine. Keep the two existing connections on its pins 1 and 2 exactly as they are, and tie the new pin 3 to GND. Keep the reference P1. Leave every other part untouched, including its position, and add no other parts. Render the schematic.
